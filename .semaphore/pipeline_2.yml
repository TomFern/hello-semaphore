version: v1.0
name: Deployment pipeilne
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Deploy to prod
    task:
      jobs:
        - name: 'Deploy '
          commands:
            - echo "job 1"
